# Shared UI

React components, hooks, types, and utilities used by both:

- `../frontend` — web app (API + PostgreSQL)
- `../extension` — Chrome extension (IndexedDB + local PDF)

Import via the `@shared` alias configured in each app's Vite/TypeScript config.
